// Blur types
export const BLUR_TYPES = {
  TEXT: 'text',
  AREA: 'area',
  ELEMENT: 'element'
};

// Default values
export const DEFAULT_BLUR_INTENSITY = 5;
export const MAX_BLUR_INTENSITY = 20;
export const MIN_BLUR_INTENSITY = 1;

// Storage keys
export const STORAGE_KEYS = {
  BLURS: 'blurs',
  ACTIVE_MODE: 'activeMode',
  BLUR_INTENSITY: 'blurIntensity'
};

// Message actions
export const MESSAGE_ACTIONS = {
  PING: 'ping',
  SET_MODE: 'setMode',
  UPDATE_BLUR_INTENSITY: 'updateBlurIntensity',
  UNBLUR: 'unblur',
  UNBLUR_ALL: 'unblurAll',
  UPDATE_BLUR_LIST: 'updateBlurList'
};

// CSS classes
export const CSS_CLASSES = {
  BLURRED: 'page-blur-pro-blurred',
  AREA: 'page-blur-pro-area',
  HIGHLIGHT: 'page-blur-pro-highlight',
  TEXT: 'page-blur-pro-text'
}; 